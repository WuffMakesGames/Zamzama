function ZamSandbox(zam) constructor {
	self.parent = zam
	
	// Variables =================================
	memory = new ZamMemory(zam)
	
	// Methods ===================================

	/// @desc Resets a region of memory
	function reload(offset, start, size=1) {
		
	}
}